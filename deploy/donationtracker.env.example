PORT=8000
# Point at your own OpenAI-compatible endpoint (Ollama, LM Studio, vLLM, llama.cpp).
LLM_BASE_URL=http://localhost:11434/v1
LLM_API_KEY=sk-local
DATA_DIR=/opt/donationtracker/data
# LLM_MODEL=qwen2.5-vl-7b-instruct      # uncomment to pin a model
# USE_HTTPS=1                            # set + provide SSL_* below to serve TLS
# SSL_KEY_FILE=/opt/donationtracker/data/certs/key.pem
# SSL_CERT_FILE=/opt/donationtracker/data/certs/cert.pem
